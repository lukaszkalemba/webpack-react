import react from '../../images/react.gif';
import './Hero.scss';

const Hero = () => {
  return (
    <div className='wrapper'>
      <img src={react} alt='' width='250' height='250' />
    </div>
  );
};

export default Hero;
